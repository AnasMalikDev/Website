// AMProTool — hero gallery tilt // Purely progressive: the page looks and works fully fine without this file. (() => { const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches; const hasHover = window.matchMedia('(hover: hover)').matches; if (reduceMotion || !hasHover) return; const devices = document.querySelectorAll('.tool-gallery .device'); devices.forEach((device) => { const img = device.querySelector('img'); if (!img) return; device.addEventListener('mousemove', (e) => { const rect = device.getBoundingClientRect(); const x = (e.clientX - rect.left) / rect.width - 0.5; const y = (e.clientY - rect.top) / rect.height - 0.5; const rotateX = (-y * 14).toFixed(2); const rotateY = (x * 14).toFixed(2); img.style.transform = scale(1.08) translateY(-6px) rotateX(${rotateX}deg) rotateY(${rotateY}deg); }); device.addEventListener('mouseleave', () => { img.style.transform = ''; }); }); })();
